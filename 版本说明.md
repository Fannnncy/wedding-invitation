# 两个封面版本

启动本地服务后，通过以下地址分别打开：

- 月夜流云版：http://localhost:4173/?version=clouds — 使用 assets/background3.png，保留从左向右循环的云层。
- 无云版：http://localhost:4173/?version=plain — 使用 assets/background.png，隐藏云层。

未指定 version 时默认显示月夜流云版。两个版本共用音乐和页面交互，但婚礼信息独立维护：流云版修改 `app-clouds.js`，无云版修改 `app-plain.js`。
